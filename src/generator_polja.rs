use rand::prelude::*;

use crate::strukture::Mreza;

impl Mreza {
    pub fn new(velikost : (u16,u16), st_min) -> Mreza {
        todo()
    }
}

fn random_array(st_vseh:u16,st_min:u16) -> Vec<u8> {
    // let enke = vec![1; st_min];
    // let nicle = vec![0; st_vseh - st_min];
    let nakljucno = vec![];
    loop {
        random_ratio()
        
    }
}